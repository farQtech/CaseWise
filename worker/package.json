{
  "name": "case-wise-worker",
  "version": "1.0.0",
  "description": "Health App Background Worker Service",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'Worker is ready to run'",
    "test": "jest",
    "lint": "eslint src/",
    "clean": "echo 'Worker cleaned'",
    "format": "prettier --write src/**/*.js",
    "type-check": "echo 'Worker type check passed (JS)'"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "dotenv": "^16.3.1",
    "pdf": "^0.1.0",
    "pdf-parse": "^1.1.1",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
