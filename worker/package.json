{
  "name": "health-app-worker",
  "version": "1.0.0",
  "description": "Health App Background Worker",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'Worker is ready to run'",
    "test": "jest",
    "lint": "eslint src/",
    "clean": "echo 'Worker cleaned'",
    "format": "prettier --write src/**/*.js",
    "type-check": "echo 'Worker type check passed (JS)'"
  },
  "dependencies": {
    "bull": "^4.12.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
